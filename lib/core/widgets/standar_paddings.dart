import 'package:flutter/widgets.dart';
import 'package:assembly/core/constants.dart';

const standardPadding = EdgeInsets.all(kStandardPadding);
const standardMinimalPadding = EdgeInsets.all(kStandardMinimalPadding);
const standardExcluddedTopPadding = EdgeInsets.only(
  left: kStandardPadding,
  right: kStandardPadding,
  bottom: kStandardPadding,
);
const standardHorizontalPadding = EdgeInsets.only(
  left: kStandardPadding,
  right: kStandardPadding,
);
